# UTF-8
#
# For more details about fixed file info 'ffi' see:
# http://msdn.microsoft.com/en-us/library/ms646997.aspx
VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers should be always a tuple with four items: (1, 2, 3, 4)
    # Set not needed items to zero 0.
    filevers=(3, 7, 15, 1),
    prodvers=(3, 7, 0, 0),
    # Contains a bitmask that specifies the valid bits 'flags'r
    mask=0x17,
    # Contains a bitmask that specifies the Boolean attributes of the file.
    flags=0x0,
    # The operating system for which this file was designed.
    # 0x4 - NT and there is no need to change it.
    OS=0x4,
    # The general type of file.
    # 0x1 - the file is a DLL.
    fileType=0x2,
    # The function of the file.
    # 0x0 - the function is not defined for this fileType
    subtype=0x0,
    # Creation date and time stamp.
    date=(0, 0)
    ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904b0',
        [StringStruct(u'CompanyName', u'TheIronWolfModding'),
        StringStruct(u'FileDescription', u'rFactor 2 Shared Memory Map Plugin'),
        StringStruct(u'FileVersion', u'3.7.15.1'),
        StringStruct(u'InternalName', u'rF2SharedMemoryMapPlugin'),
        StringStruct(u'LegalCopyright', u'GNU General Public License v3.0'),
        StringStruct(u'OriginalFilename', u'rF2SharedMemoryMapPlugin64.dll'),
        StringStruct(u'ProductName', u'rF2SharedMemoryMapPlugin'),
        StringStruct(u'ProductVersion', u'3.7.0.0')])
      ]),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)




























